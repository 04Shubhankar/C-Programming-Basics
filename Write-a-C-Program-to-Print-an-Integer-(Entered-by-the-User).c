//Write a C Program to Print an Integer (Entered by the User)
#include <stdio.h>
int main(){
    int in;
    printf("Enter a Intiger:");
    scanf("%d",&in);
    printf("\nThe Entered Intiger is %d",in);
    return 0;
}